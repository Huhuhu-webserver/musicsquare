# 皮卡丘音乐站 EXE 打包指南

## 项目概述

本项目是皮卡丘音乐站的 EXE 打包版本，基于纯 HTML 版本改造，添加了以下功能：

- 增加本地播放模式
- 网易云热门歌单功能
- 本地 EXE 运行支持
- 增加批量下载功能
- 增加收藏，歌单记录缓存
- 增加版本更新功能
- 增加歌单云分享和本地分享功能

当前版本：**1.1.9**

## 环境要求

1. **Python 版本**：Python 3.13.0+（建议使用 3.13.0 版本）
2. **依赖包版本**：
```powershell
# 安装依赖
pip install pyinstaller==6.10.0 pywebview==5.0.5 pythonnet==3.0.4

# 可选：安装其他可能需要的依赖
pip install requests==2.32.3
```

## 打包步骤

### 1. 准备工作

确保项目目录结构如下：
```
musicsquare-main-pc打包exe版本/
├── index.html          # 主页面
├── local.html          # 本地音乐页面
├── style.css           # 主样式
├── pikachu.gif         # 图标
├── wss.py              # WebSocket 服务
├── docs/
│   ├── logo.png        # 应用图标
│   └── logo.ico        # EXE 图标
├── main.py             # Python 入口文件
├── 皮卡丘音乐站.spec    # PyInstaller 配置文件
└── version.json        # 版本信息
```

### 2. 执行打包命令

```powershell
# 清理旧文件
Remove-Item -Path "dist" -Recurse -Force -ErrorAction SilentlyContinue
Remove-Item -Path "build" -Recurse -Force -ErrorAction SilentlyContinue

# 执行打包
pyinstaller "皮卡丘音乐站.spec"
```

### 3. 查看结果

打包完成后，EXE 文件位于：
```
dist/pikachu_music-v1.1.9.exe
```

## 注意事项

1. **图标要求**：确保 `docs/logo.ico` 存在作为 EXE 图标
2. **WebView2**：EXE 运行时需要安装 WebView2 运行时（Windows 10/11 通常已预装）
3. **Python 版本**：建议使用 Python 3.13
4. **依赖版本**：
   - pyinstaller
   - pywebview
   - pythonnet
5. **编码处理**：项目已修复非 ASCII 字符（如韩语）的编码错误问题
6. **功能支持**：支持多语言歌单、热门榜单、本地音乐等功能

## 版本更新日志

### v1.1.9
- 版本升级到 1.1.9
- 增加了其他语种的音乐榜单支持
- 修复了热门歌单切换后下一首按钮失效的问题
- 修复了搜索韩语歌曲时的编码错误问题

### v1.1.8
- 修复咪咕音乐在 EXE 中无法显示歌词的问题
- 优化歌词获取逻辑
- 额外增加了对粤语歌单的支持

### v1.1.7
- 修复咪咕音乐无法播放的问题
- 优化日志输出
